<Page x:Class="NimCSharp.OpponentList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="600"
	  Title="OpponentList">
    
    <Page.Resources>
        <Style x:Key="Blue-Button" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="BorderButton" CornerRadius="10" BorderThickness="3" Background="#FF2669BF" BorderBrush="#FF0A2F5F">
                            <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" Value="#1F57A0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize" Value="12pt"/>
            <Setter Property="FontFamily" Value="Helvetica"/>
            <Setter Property="Foreground" Value="#FFD9D0C7"/>
        </Style>
        
        <Style x:Key="ListBoxBorder" TargetType="ListBox">
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="Background" Value="#E1D9D2"/>
            <Setter Property="BorderBrush" Value="#AEA69F"/>
            <Setter Property="Background" Value="#E1D9D2"/>
        </Style>
        <Style x:Key="ListBoxHeader">
            <!--<Setter Property="FrameworkElement.Visibility" Value="Collapsed"/>-->
        </Style>
    </Page.Resources>
    <Grid>
        <Label Content="CHOOSE A CHALLENGER" Foreground="#FF2669BF" HorizontalAlignment="Center" VerticalAlignment="Top" FontFamily="/NimCSharp;component/#Intro" FontSize="40" Margin="40,0" Width="512"/>
        <ListView ItemsSource="{Binding OpponentCollection}"
                  Foreground="#454545"
                  HorizontalAlignment="Center" 
                  Width="520" Height="242" 
                  VerticalAlignment="Top" 
                  Margin="32,63,32,0"
                  Style="{StaticResource ListBoxBorder}" SelectionChanged="ListView_SelectionChanged">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Height" Value="20"/>
                    <Setter Property="Foreground" Value="#454545"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Opponent Name" HeaderContainerStyle="{StaticResource ListBoxHeader}" Width="300" DisplayMemberBinding="{Binding OpponentName}"/>
                    <GridViewColumn Header="IP Address" HeaderContainerStyle="{StaticResource ListBoxHeader}" Width="100" DisplayMemberBinding="{Binding IPAddress}"/>
                    <GridViewColumn HeaderContainerStyle="{StaticResource ListBoxHeader}" Width="50" DisplayMemberBinding="{Binding InviteArea}"/>
                    <!--<GridViewColumn HeaderContainerStyle="{StaticResource ListBoxHeader}" Width="700"/>-->
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Page>
